# Architecture

How **underwriting-copilot** is built *right now* — the moving parts, how they fit,
key data flows, where things live, external services, non-obvious constraints.

A **state** document: overwrite it as the architecture changes. The *reasons*
the architecture is shaped this way live in `decisions.md`; the *story* of how
it got there lives in `journal.md`. This file just says what *is*.

## Components

- `src/` — the importable package.
<!-- describe modules as they appear -->

## Data flow

<!-- how data moves through the system -->

## External services & configuration

<!-- APIs, env vars, what they control -->

## Constraints worth knowing

<!-- the non-obvious things that bite newcomers -->
